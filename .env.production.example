# ==============================================================================
# PingPoint Production Environment Variables
# ==============================================================================
# IMPORTANT:
# - Copy this file to .env (for docker-compose) or .env.production
# - Fill in real values - DO NOT commit secrets to version control
# - Generate secrets with: openssl rand -base64 32
# ==============================================================================

NODE_ENV=production
PORT=8080

# Database (docker-compose uses "db" as hostname)
DATABASE_URL=postgres://pingpoint:pingpoint_password@db:5432/pingpoint
POSTGRES_PASSWORD=pingpoint_password

# JWT Secret (REQUIRED - must be 32+ characters)
JWT_SECRET=change_this_to_a_long_random_string_min_32_chars

# Email (Resend API - https://resend.com)
RESEND_API_KEY=re_your_resend_api_key
MAIL_FROM=no-reply@yourdomain.com

# Public URL (used in email links)
# For local docker: http://localhost:8080
# For production: https://yourdomain.com
PINGPOINT_PUBLIC_URL=http://localhost:8080
