# ==============================================================================
# PingPoint Production Environment Variables
# ==============================================================================
# IMPORTANT: 
# 1. Copy this file to .env (for docker-compose to read)
# 2. Fill in actual values - DO NOT commit real secrets to version control
# 3. Generate strong secrets: openssl rand -base64 32
# ==============================================================================

# Application
NODE_ENV=production
PORT=8080

# Database
# For docker-compose: use db:5432 as the host (container name)
# For standalone: use your actual database host
DATABASE_URL=postgres://pingpoint:your_secure_password@db:5432/pingpoint
POSTGRES_PASSWORD=your_secure_password

# JWT Authentication
# REQUIRED: Generate a strong 32+ character secret
# Run: openssl rand -base64 32
JWT_SECRET=change_this_to_a_long_random_string_min_32_chars

# Session cookie name (optional, has default)
SESSION_COOKIE_NAME=pingpoint_broker_session

# Email Configuration (Resend)
# Get your API key from https://resend.com
RESEND_API_KEY=re_your_resend_api_key

# Public URL (REQUIRED for email links)
# Set this to your actual domain with https://
PINGPOINT_PUBLIC_URL=https://yourdomain.com

# ==============================================================================
# Optional Configuration (defaults shown)
# ==============================================================================

# Rate Limiting
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100

# Token Expiry
# JWT_EXPIRY_DAYS=30
# VERIFICATION_TOKEN_EXPIRY_DAYS=2

# ==============================================================================
# Docker Compose Specific
# ==============================================================================
# POSTGRES_USER=pingpoint  (defaults to pingpoint in docker-compose.yml)
# POSTGRES_DB=pingpoint    (defaults to pingpoint in docker-compose.yml)
