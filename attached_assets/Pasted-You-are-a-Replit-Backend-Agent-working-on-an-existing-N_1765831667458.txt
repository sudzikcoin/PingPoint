You are a Replit Backend Agent working on an existing Next.js / Node.js project (PingPoint / Suverse).  
CRITICAL: 
- Делай МИНИМАЛЬНЫЕ локальные правки, не ломай существующую архитектуру, роуты, схемы БД и env-переменные.  
- Ничего не переписывай “с нуля”. Меняй только то, о чём прямо сказано.  
- Не добавляй новых зависимостей, если можно обойтись текущими.

ЗАДАЧА: изменить адрес отправителя писем Resend так, чтобы все исходящие письма шли с домена `suverse.io`, и конкретно с адреса `info@suverse.io`.

1. Найди модуль(и), где используется Resend:
   - обычно это что-то вроде `import { Resend } from 'resend'` или `const resend = new Resend(process.env.RESEND_API_KEY)`;
   - нас интересуют вызовы `resend.emails.send({ ... })` для:
     - верификационных писем брокерам / водителям,
     - любых других писем, связанных с PingPoint.

2. В каждом вызове `resend.emails.send`:
   - НАЙДИ существующее поле `from: ...`
     - сейчас оно может выглядеть как:
       - `'PingPoint <onboarding@resend.dev>'`
       - или другой адрес, не относящийся к `@suverse.io`.
   - ЗАМЕНИ значение поля `from` на:

     ```ts
     from: 'PingPoint <info@suverse.io>',
     ```

   - Если поля `from` нет, аккуратно добавь его в объект конфигурации письма.

3. Если в проекте уже используется env-переменная для адреса отправителя (например, `process.env.EMAIL_FROM`):
   - обнови её дефолт так, чтобы по умолчанию был `info@suverse.io`:
     ```ts
     from: process.env.EMAIL_FROM || 'PingPoint <info@suverse.io>',
     ```
   - но НЕ вводи новую переменную и не меняй формат существующих без необходимости.
   - Не меняй другие env-имена (RESEND_API_KEY и т.п.).

4. Убедись, что:
   - во ВСЕХ местах, где Resend отправляет e-mail во внешние адреса (брокеры, водители, клиенты), `from` теперь содержит `info@suverse.io`;
   - тестовые отправки на `sudzikgroup@gmail.com` тоже используют этот же `from`.

5. Ничего не трогай в DNS, конфиге Resend и других частях проекта: этой задачей занимается сам пользователь.

6. После правок:
   - если в проекте есть простые автотесты / health-check, запусти их;
   - в консоли выведи краткий лог, какие файлы и строки были изменены (например: `Updated Resend "from" in src/server/email/sendVerificationEmail.ts`).

НЕ ДЕЛАЙ больше никаких изменений, кроме описанных выше.